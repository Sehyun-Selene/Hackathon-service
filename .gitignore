# 의존성 (배포 시 자동 설치되므로 올리지 않음)
node_modules/

# 빌드 산출물 (배포 시 자동 생성)
dist/

# 로그 · OS 파일
*.log
.DS_Store
Thumbs.db

# 로컬 미리보기 임시
.vite/

# ── 작업용 원본 자료 ─────────────────────────────────────────────
# 앱이 실제로 쓰는 이미지는 participant-app/public/ 과 src/assets/ 안에
# 있고 이미 커밋되어 있습니다. 아래는 그것을 만들기 위한 원본·참고
# 자료라 저장소에 넣지 않습니다 (다 합치면 90MB가 넘습니다).

# 저장소 뿌리에 둔 원본 이미지 — 로고, 타임테이블·음식 여정 원본 등.
# 뿌리 한 칸만 걸러서, 앱 안(participant-app/ · admin-app/)의 이미지는
# 앞으로 추가하는 것까지 그대로 커밋됩니다.
/*.png

# 디자인 캔버스 원본과 내려받은 사본 (26MB + 26MB)
PLAI ORDER app design/
PLAI ORDER app design.zip

# 음식 사진·일러스트 원본, 손으로 찍어둔 화면 (22MB + 11MB + 11MB)
음식 사진/
음식 일러스트/
manual-screenshots/

# 관리자 앱 재디자인 원본 (1.3MB)
Hackathon service admin redesign.zip
